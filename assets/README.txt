Vui lòng đặt file `icon.ico` và `alert.wav` vào thư mục này.
Nếu không có, ứng dụng sẽ sử dụng icon mặc định và âm thanh hệ thống.
Bạn có thể chạy `python setup_assets.py` để tự động tạo file giả lập.
